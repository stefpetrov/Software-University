function projects(input) {
    let nameArchitect = input[0];
    let numberOfProjects = input[1];
    let timeProjects = input[1] * 3;

console.log(`The architect ${nameArchitect} will need ${timeProjects} hours to complete ${numberOfProjects} project/s.`)

}

projects(["George", "4", ])

